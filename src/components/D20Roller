const D20Roller = (props) => {
  const { label, onRoll, notation } = props;

  const roll = () => {
    onRoll(notation);
  };

  return <button onClick={roll}>{label}</button>;
};

export default D20Roller;
