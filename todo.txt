Features:
- Grouped initiative AC's and HP's
- Legendary monster toggle, with legendary action points and resistances
- Preset soundboard
- Add carousel for all uploaded grid images (buttons in footer)
- Favorite character button that saves a cookie with gridrow info
- triggers on names
- saves into cookie, defaults are CoC characters
- Needs little save button per row
- Needs a way to delete old ones in modal


 
1.1 5-26: A stat block maker that will allow you to save it in a DB and then select it in the initative tracker such that its stat block appears in the images.
This will then allow you to import them into the initiative tracker easy

Needs a database and full editor 
Any value is overwritable, but is also set automatically

Todo 1.2:
- database
    - This allows me to save stat blocks for quick ref and to put into the init tracker automatically on typing its name
Todo 1.3
    - Add saving the json of a stat block to DB, then in initative tracker allow auto importing it
    - Undo system
    - Reactions, bonus actions
    - Inverntory/loot
    - Lair actions

